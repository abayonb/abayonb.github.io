const pathToImage = `${process.env.PUBLIC_URL}/assets/pictures`;

export const siteComResources = () => ({
  title: 'home.country.title',
  placeholder: 'home.country.placeholder',
  imageUrl: `${pathToImage}/picture--landing.jpg`,
});
